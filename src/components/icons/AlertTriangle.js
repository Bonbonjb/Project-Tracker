// src/components/ui/icons/AlertTriangle.js
import { AlertTriangle as AlertTriangleIcon } from 'lucide-react';

const AlertTriangle = (props) => <AlertTriangleIcon {...props} />;

export default AlertTriangle;
